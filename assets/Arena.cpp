#include "Arena.h"

Arena::Arena(wxWindow *parent,
            wxWindowID winid,
            const wxPoint& pos,
            const wxSize& size,
            long style,
            const wxString& name): wxGrid(parent, winid, pos, size, style, name)
{
    //ctor
}

Arena::~Arena()
{
    //dtor
}
